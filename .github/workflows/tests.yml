name: test suite
on: push
jobs:
  run tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: install dependencies
        run: yarn
      - name: run test suite
        run: yarn test
